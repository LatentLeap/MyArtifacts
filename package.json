{
  "name": "myartifacts",
  "version": "0.1.0",
  "description": "Self-hosted Claude Code Artifacts with client annotations: publish a page from an agent session, hand a customer a link, read what they pinned on it.",
  "private": true,
  "license": "MIT",
  "repository": "github:LatentLeap/MyArtifacts",
  "type": "module",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "start": "node src/server.ts",
    "mint-token": "node src/server.ts mint-token",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "marked": "^18.0.11"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2"
  }
}
